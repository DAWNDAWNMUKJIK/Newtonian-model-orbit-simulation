# Binary System Orbit Simulation

## Binary System's Orbit: Collision, Elliptical, Circular, and Escape

### Team Members
- Ray Fang
- Taeho Kim
- Hejie Guan
- Aurélien Fang

## Description

This project simulates the orbit of a binary star system.

The binary star system can result in:

- Collision
- Bounded orbit (elliptical)
- Circular orbit
- Escape

These orbital behaviors depend on the relative velocity of the two stars, particularly compared with velocities such as the circular velocity (`v_circ`) and escape velocity (`v_esc`).

## Before Running

Before running the code, download/install the required Python libraries:
pip install astropy
pip install numpy
pip install scipy
pip install matplotlib
